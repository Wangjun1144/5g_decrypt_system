/**
 * UE-context capability boundary inside the processing layer.
 *
 * <p>This package owns the service, repository port, request/result types, and
 * context-specific events. Message-specific update execution lives in the
 * {@code update} subpackage.</p>
 */
package com.example.procedure.processing.context;
