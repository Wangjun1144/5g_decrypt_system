/**
 * UE-context update execution chain.
 *
 * This package contains message-specific updaters, the dispatcher that selects
 * them, and shared update support such as mapping writes and key-derivation
 * helpers. The root {@code context} package keeps the service and repository
 * boundaries, while this subpackage owns execution details.
 */
// REFACTOR STEP: CONTEXT_SUBPACKAGE_REORG
package com.example.procedure.processing.context.update;
