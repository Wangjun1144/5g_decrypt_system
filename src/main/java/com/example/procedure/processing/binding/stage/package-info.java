/**
 * binding 阶段入口子包。
 *
 * 当前定位：
 * 1. 放置阶段请求/结果模型与对外处理入口
 * 2. 让 binding 主入口与底层解析/事件发布分离
 * 3. 为后续把 binding stage 独立成模块或服务接口预留清晰边界
 */
// REFACTOR STEP: BINDING_SUBPACKAGE_REORG
package com.example.procedure.processing.binding.stage;
