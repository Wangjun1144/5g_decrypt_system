/**
 * 流程识别子域中的 flow 运行期组件包。
 *
 * 当前定位：
 * 1. 这里放流程匹配、关闭策略、运行期注册入口等执行期组件
 * 2. 旧的 com.example.procedure.flow 包会逐步退成兼容层
 * 3. processing.procedure 应优先依赖这里的正式入口
 */
// REFACTOR STEP: FLOW_RUNTIME_REORG
package com.example.procedure.processing.procedure.flow;
