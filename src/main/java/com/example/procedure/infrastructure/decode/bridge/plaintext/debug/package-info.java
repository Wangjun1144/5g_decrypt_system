/**
 * Debug-only artifacts for plaintext decode/build flows.
 *
 * These objects are intended for diagnostics, local verification, and test
 * scenarios rather than the core production decode path.
 */
package com.example.procedure.infrastructure.decode.bridge.plaintext.debug;
