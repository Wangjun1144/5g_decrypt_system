/**
 * Decode bridge root package.
 *
 * The root package now serves mainly as the umbrella for capability-oriented
 * subpackages such as build, json, pcap, plaintext, and reentry. Concrete
 * bridge contracts and implementations should live in those subpackages rather
 * than directly in this root namespace.
 */
package com.example.procedure.infrastructure.decode.bridge;
