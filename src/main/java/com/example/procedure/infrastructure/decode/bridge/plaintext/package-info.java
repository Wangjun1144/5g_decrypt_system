/**
 * Plaintext re-decode bridge boundaries.
 *
 * This package owns the dedicated flow for turning plaintext hex into pcap and
 * then into JSON or parsed signaling messages. It keeps this specialized
 * workflow separate from the more general pcap and JSON bridge contracts.
 */
// REFACTOR STEP: DECODEBRIDGE_PLAINTEXT_SUBPACKAGE
package com.example.procedure.infrastructure.decode.bridge.plaintext;
