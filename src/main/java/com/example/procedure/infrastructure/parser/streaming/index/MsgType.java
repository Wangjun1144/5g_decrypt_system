package com.example.procedure.infrastructure.parser.streaming.index;

/**
 * Node types tracked inside a streaming chain index.
 */
public enum MsgType {
    PACKET,
    MAC,
    PDCP,
    RRC,
    NAS,
    NGAP,
    NUAR
}
