/**
 * Streaming chain index model.
 *
 * This package contains the lightweight index structures built during
 * incremental tshark JSON parsing. The index records packet-local hierarchy,
 * payload ordering, and type-based lookup information so parsed streaming
 * chains can later be turned into normalized message trees.
 */
package com.example.procedure.infrastructure.parser.streaming.index;
