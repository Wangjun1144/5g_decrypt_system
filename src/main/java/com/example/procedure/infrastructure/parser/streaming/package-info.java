/**
 * Streaming parse pipeline support.
 *
 * This module contains the preferred incremental parsing path for tshark JSON.
 * The root package exposes the formal streaming parser boundary, while
 * subpackages contain chain indexes, layer-level selective parsing, and
 * message-tree assembly helpers.
 */
package com.example.procedure.infrastructure.parser.streaming;
