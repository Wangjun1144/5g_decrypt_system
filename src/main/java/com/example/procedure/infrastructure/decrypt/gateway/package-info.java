/**
 * Transport-neutral decrypt gateway contracts.
 *
 * This package exposes the stable decrypt capability boundary used by the
 * processing pipeline. Concrete transport implementations, such as HTTP, stay
 * in subpackages so upper layers do not depend on transport-specific DTOs.
 */
package com.example.procedure.infrastructure.decrypt.gateway;
