/**
 * Offline capture-file readers inspired by Wireshark's wiretap split.
 *
 * <p>This package is intentionally isolated from protocol decoding so the
 * project can evolve from "read capture" to "decode protocols" in separate
 * migration steps.</p>
 */
package com.example.procedure.infrastructure.capture;
