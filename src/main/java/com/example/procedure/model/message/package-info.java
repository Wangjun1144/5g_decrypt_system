/**
 * Message-specific model objects nested under the main signaling message model.
 *
 * This package contains message-local metadata, decrypt lifecycle state, and
 * payload abstractions that are specific to one signaling message instance.
 */
package com.example.procedure.model.message;
