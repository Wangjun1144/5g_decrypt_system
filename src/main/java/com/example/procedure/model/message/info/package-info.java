/**
 * Parse-result information objects produced by packet and JSON parsers.
 *
 * These types describe protocol-layer facts extracted during parsing and are
 * distinct from the parser implementations that build them.
 */
package com.example.procedure.model.message.info;
