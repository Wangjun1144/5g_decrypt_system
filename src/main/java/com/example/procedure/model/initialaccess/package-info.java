/**
 * Initial-access specific payload models extracted from signaling messages.
 *
 * These types represent the payloads that drive the initial access procedure
 * and are kept separate from the generic {@code model.message} abstractions.
 */
package com.example.procedure.model.initialaccess;
