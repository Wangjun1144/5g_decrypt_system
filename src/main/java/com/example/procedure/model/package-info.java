/**
 * Core domain and pipeline result models shared across the system.
 *
 * This package keeps cross-cutting message, procedure, score, and context
 * models that are not owned by a narrower subpackage such as
 * {@code model.message}.
 */
package com.example.procedure.model;
