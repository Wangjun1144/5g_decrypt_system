/**
 * Shared result-contract types used across application and processing layers.
 *
 * <p>These contracts are intentionally modeled outside the processing package
 * so summaries and operation outcomes can be exchanged without creating a
 * reverse dependency back into one specific subdomain.</p>
 */
package com.example.procedure.model.result;
