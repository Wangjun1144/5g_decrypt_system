/**
 * Application-level pcap ingestion entry points.
 *
 * This package owns the application-facing contracts and orchestrators for one
 * pcap ingestion run. It sits above decode bridging and below external callers
 * such as tests, batch jobs, or future API adapters.
 */
package com.example.procedure.application.pcap;
