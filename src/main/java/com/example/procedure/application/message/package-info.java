/**
 * Application-level signaling-message ingress boundaries.
 *
 * This package contains the contracts and ingress request models for one
 * signaling message entering the application pipeline, regardless of whether
 * the source is direct input, pcap ingestion, or reentry from downstream
 * processing.
 */
package com.example.procedure.application.message;
