/**
 * Application-layer entry points and orchestration contracts.
 *
 * This package owns inbound requests, stage-level application exceptions, and
 * top-level coordinators that drive the lower processing subdomains.
 */
package com.example.procedure.application;
